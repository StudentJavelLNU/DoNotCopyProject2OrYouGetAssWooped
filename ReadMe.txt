SimulationTime can be entered as command line argument or changed in code.
Main prints debuginformation, can be changed in main()
Tests does not print debuginformation, and neighter does it pass the tests.
It passes some values, some are close, others not.

Solution does have some deadlock prevention, but a high starvationRate.
